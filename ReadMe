程序运行说明：
第一组：服务器-客户端重放测试
1.运行Server_Long，开启服务器；
2.运行Client_Long，客户端连接服务器，在控制台输入任意消息发送给服务器，输入bye断开连接，输入exit运行结束；
3.运行Client_Re，开启监听程序，监听程序录制自开启后10秒内的通信数据，发送给服务器，发送完毕后程序结束。
第二组：Access服务器-Access客户端重放测试
1.运行AccessServer，开启服务器，端口号7777；
2.运行AccessClient，客户端连接服务器，在控制台输入sql语句修改数据表；
3.运行AccessClient_Re，开启监听程序，监听程序录制自开启后10秒内的通信数据，发送给服务器，发送完毕后程序结束。
第三组：MySQL客户端
1.运行MySQLTest，在主线程中完成测试。